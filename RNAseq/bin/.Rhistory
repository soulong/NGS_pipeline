# load packages
package_need <- c("yaml", "here", "tximport", "tidyverse", "writexl", "readxl")
sapply(package_need, function(x) library(x, character.only=T))
sapply(package_need, function(x) require(x, character.only=T))
# load packages
package_need <- c("yaml", "here", "tximport", "tidyverse", "writexl", "readxl")
sapply(package_need, function(x) require(x, character.only=T))
